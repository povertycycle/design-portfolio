import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    basePath: "/design-portfolio",
    output: "export",
    allowedDevOrigins: ["192.168.0.119"],
};

export default nextConfig;
