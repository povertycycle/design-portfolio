export interface NewsData {
    header: string;
    summary: string;
    coverArt: string;
    postedAt: string;
}

export interface SimilarStories {
    header: string;
    coverArt: string;
    postedAt: string;
}
